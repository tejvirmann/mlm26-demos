# 1. See what's already connected
$ claude mcp list

# 2. Add a server (CLI, or edit .mcp.json / claude_desktop_config.json)
$ claude mcp add github -- npx -y @modelcontextprotocol/server-github

# 3. Reload the client, then confirm it's listed
$ claude mcp list
  github   connected

# 4. Just ask — the agent picks the right tool itself
> what are the open issues in this repo?